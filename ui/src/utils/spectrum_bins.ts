/**
 * Log-spectrum viz bin clamp — keep in sync with
 * `calfNXT::Dsp::kMaxSpectrumBins` / `kMinSpectrumBins` (`common/dsp/spectrum_bins.h`).
 */
export const SPECTRUM_MAX_BINS = 512;
export const SPECTRUM_MIN_BINS = 32;
